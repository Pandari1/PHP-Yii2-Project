version: '3.7'

services:
  yii2app:
    image: pandu321/yii2-app:latest
    ports:
      - "8080:80"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - yii2app_net

networks:
  yii2app_net:
    driver: overlay
